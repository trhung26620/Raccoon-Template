info:
  id: wp-plugins-detect
  name: WordPress Plugins Detection
  author: Hung, Dat, Hoa, Danh
  severity: info
  tags: fuzz,wordpress

requests:
  - request:
      - |
        GET /wp-content/plugins/{{pluginSlug}}/readme.txt HTTP/1.1
        Host: {{Hostname}}

    payloads:
      pluginSlug: ../Wordlists/wp-plugins.txt
    scanMode: batteringram
    thread: 50
    multipleMatcher: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        word:
          - "== Description =="
